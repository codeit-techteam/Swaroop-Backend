import { Module } from '@nestjs/common';

/**
 * Phase 2 domain boundary for Products.
 * Business REST controllers/services are intentionally deferred to later phases.
 */
@Module({})
export class ProductsModule {}
